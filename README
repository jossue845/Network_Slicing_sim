The code show below is a GUI so it needs the .png files to work. The code that contains all the functions for this simulation to work is "GuiFig". The GUI can be started from the "Caratula" script which will show the author's and project's name.
